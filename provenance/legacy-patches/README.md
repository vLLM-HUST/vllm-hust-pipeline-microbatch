# Legacy patch archive

This directory preserves original commits from core PR #135 and Ascend PR #136
as unmodified `git format-patch` exports, including benchmark tooling and
historical artifacts.

Historical results are not evidence for a newly supported host version.
Scheduler and worker changes need explicit host contracts before activation.
